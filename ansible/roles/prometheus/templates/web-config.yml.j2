{{ ansible_managed | comment }}

{% if prometheus_tls_enabled %}
tls_server_config:
  cert_file: {{ prometheus_tls_cert_path }}
  key_file: {{ prometheus_tls_key_path }}

{% endif %}
{% if prometheus_basic_auth_enabled %}
basic_auth_users:
  {{ prometheus_basic_auth_username }}: "{{ _prometheus_basic_auth_password_hash }}"
{% endif %}
