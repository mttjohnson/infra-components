# Incus Trust

This role will setup the Incus client tools and setup authentiction to a remote

- Install Incus Client
- Generate self signed TLS credentials
- Authorize (trust) client TLS cert on remote Incus host
- Add new authorized remote and set as default remote
